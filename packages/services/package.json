{
  "name": "services",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "shell": "firebase functions:shell",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions,firestore,storage --force",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^9.2.0",
    "firebase-functions": "^3.11.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.2.0",
    "firebase-tools": "^9.5.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.8.0"
  },
  "engines": {
    "node": "12"
  }
}
